.env
.env.example
__pycache__/
*.pyc

# Raw HuggingFace dataset shards used for local seeding — too large for git
# (single shard is 400MB+; GitHub hard-rejects any file over 100MB anyway).
data/hf_hires/

