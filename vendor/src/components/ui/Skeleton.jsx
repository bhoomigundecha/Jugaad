/**
 * Skeleton — a single shimmering placeholder block. Compose with layout
 * classes/styles to build up loading-state pages.
 */
export default function Skeleton({ className = '', style = {} }) {
  return <div className={`skeleton rounded-2xl ${className}`} style={style} />;
}
